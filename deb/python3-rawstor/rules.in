#!/usr/bin/make -f

export LD_LIBRARY_PATH := $(CURDIR)/../src/.libs:$(LD_LIBRARY_PATH)

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	dh_auto_test

override_dh_shlibdeps:
	dh_shlibdeps -l$(CURDIR)/../src/.libs -- -xlibrawstor.so.@PACKAGE_VERSION_MAJOR@
