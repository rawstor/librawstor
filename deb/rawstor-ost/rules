#!/usr/bin/make -f

%:
	dh $@

override_dh_clean:

override_dh_autoreconf:

override_dh_auto_configure:
	dh_auto_configure -- --without-python3

override_dh_auto_install:
	$(MAKE) -C ost install DESTDIR=$(CURDIR)/debian/$(PACKAGE)
