# rawstor-vhost-qemu

A vhost-user-blk backend built on qemu's `libvhost-user` library (see
`3rdparty/qemu/libvhost-user`), kept alongside `vhost/` (the native
protocol implementation) for performance comparison between the two.
